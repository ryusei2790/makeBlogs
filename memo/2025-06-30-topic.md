# 2025-06-30 今日考えたこと・アイデア

## 今日の日付
2025年6月30日

## 考えたこと・アイデア

### 1. 
- **目的**：LINE風のチャット機能を既存のNext.jsアプリの一部として実装
- **技術選定**：
    - Next.js (App Router)
    - Tailwind CSS
    - Firebase（チャット・画像・ユーザー情報管理）
    - Googleログイン（NextAuth.js）
    - Vercel（デプロイ）
### 2. 
### 試したこと：

- Google Cloud ConsoleでOAuthクレデンシャルを作成
- `NEXTAUTH_URL=http://localhost:3000`
- `NEXTAUTH_SECRET`にランダム文字列を設定
### 3. 
- OAuthの承認済みリダイレクトURI」が不明
    - ✅ 解決：`http://localhost:3000/api/auth/callback/google` を設定
    ### 試したこと：

- layout.tsxに `SessionProvider` を配置
- Headerコンポーネントから `LoginButton` を呼び出し

### ⚠️ エラー：

```
vbnet
コードをコピーする
Error: React Context is unavailable in Server Components

```

### ✅ 解決：

- `Header.tsx` に `"use client"` を追加してClient Componentに変更
- `LoginButton.tsx` にも `"use client"` を追加
## 技術的なアイデア

### 

## ブログ記事のアイデア

### 

## その他のメモ

### 

---
*このファイルは日々のアイデアや考えたことを記録するためのメモです。* 